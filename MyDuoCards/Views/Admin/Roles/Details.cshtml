@model MyDuoCards.Models.DBModels.Role

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<h4>Role</h4>
<hr />

<dl class="row">
    <dt class = "col-sm-2">
        <span class="text-light">
            <strong>@Html.DisplayNameFor(model => model.Name)</strong>
        </span>
    </dt>
    <dd class = "col-sm-10">
        <span class="text-dark">
            <strong>@Html.DisplayFor(model => model.Name)</strong>
        </span>
    </dd>
</dl>

<div class="form-group d-flex" style="margin-top: 10px">
    <a class="btn btn-light" style="margin-right: 10px" asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
    <a class="btn btn-light" asp-action="Index">Back to List</a>
</div>
